// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	site: 'https://asipofcoffe.com',
	// No "base" needed since we're using a custom domain (not a subpath)
});
